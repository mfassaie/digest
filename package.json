{
  "name": "webfetch-plus",
  "version": "0.1.0",
  "type": "module",
  "description": "Timeout-safe MCP server replacing Claude Code's built-in WebFetch",
  "main": "dist/index.js",
  "bin": {
    "webfetch-plus": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "defuddle": "^0.15.0",
    "linkedom": "^0.18.12",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^4.1.2",
    "mcp-testing-kit": "^0.2.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
