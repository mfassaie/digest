{
  "name": "digest-workspace",
  "private": true,
  "type": "module",
  "description": "Workspace root for digest (MCP server, in-image service, converter eval)",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @mfassaie/digest run build",
    "lint": "pnpm --filter @mfassaie/digest run lint",
    "test": "pnpm --filter @mfassaie/digest run test"
  }
}
