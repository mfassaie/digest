{
  "name": "@digest/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP tool surface for digest: tool definitions, dispatch handlers and response formatting",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@digest/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^4.1.9",
    "mcp-testing-kit": "0.2.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
